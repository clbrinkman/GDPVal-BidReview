# 交互记录与成果

标书审查 Benchmark + 数据治理平台的构造历程（2026-08 至 2026-09），按关键决策点记录。

## 1. 立项：GDPval 能不能平移到标书审查

起点是讨论 OpenAI GDPval（职业交付物 + 盲评对拍 + 分布对齐）能否迁移。
结论：做**审查类**而不是生成类——模型当审查员，检出投标文件中的问题。
标书评判中除主观裁量外的部分（资格、符合性、废标条款）在公开招标文件里都有据可查，可形成 rubric。
关键约束：**真实投标文件永不公开** → 只能注入式合成，偏差自带 GT，这反而让"检出率/误报率"成为免费指标。

## 2. 数据治理设计（先于平台）

- 统一编号：TDR/RBC/CASE/INJ/ERR/RUN 六类前缀，只增不改
- 项目分类：工程/货物/服务；错误分类：QUA/FMT/SUB/PRC/DOC 五类法典
- 评分：异构 rubric 下逐条评分、绝不跨 rubric 合总分；指标只在同版本 case_set 上比
- HITL 矩阵：错误类型 × 项目类别，构造方式 P(程序化)/G(生成式)/R(真实案例)，人工分级 L0-L3

## 3. 爬虫：真实招标文件

`scraper/ccgp.py` 抓中国政府采购网公开招标频道，48 个项目（公告正文 + 26 个附件）。
踩过的坑：搜索接口有频控弃用改静态频道页；页面 UTF-8 但 ASCII 结构按 GBK 解码不报错 →
静默乱码入库，改 utf-8 优先 + gb18030 兜底并清库重抓；Windows 控制台打印私有区字符崩溃 → stdout reconfigure。

## 4. 平台定位的一次重大纠偏

第一版把人工队列做成了**采集人工**（附件筛选、GT 校验、裁决三个队列）。
用户纠正：要的是**后期人工** prototype，只有三件事——
① 审核生成文件是否符合要求（按生成关键节点组织，跳转校验）
② 修改调整规则
③ 质检（机器替代不了的判断）
分类、识别这类确定性工作不设人工队列，走规则（标定小样本→测规则准确率→撤人）。

由此定下平台骨架：
- 生成节点 **N1 输入解析 → N2 基线合规 → N3 偏差注入 → N4 成稿质量**，任一驳回即回炉
- 专家四区 **law / scheme / plaus / inj**（机器替代不了的判断）
- **环境文件**：agent 取证评测的噪声环境，非主审，只留按页反馈入口

## 5. 参考 LH pipeline 的两次吸收

参考另一个 human-in-loop 项目（restaurant_launch 的 human_schema + human_view）：
- schema 层：key_information 的 source/golden_path（证据路径）→ 升级为 env_file.has_evidence + 页码级反馈；
  env.files 的 what_is_it、反馈法典（缺失/错误/噪声不合理/版本不对/扫描件不真实）
- 视觉层：先复刻 human_view 全套视觉词汇（sidebar 目录树、锚点绿条、stage-card、琥珀备注卡、
  三色 item 条），后按"越简单越好"削掉技术 ID 装饰，保留卡片与色彩语义

## 6. 交互迭代（本阶段）

1. **回退**：所有判定可撤销回待审（review/reset）；环境反馈可清除
2. **编号系统入 README**，UI 去掉 mono ID 装饰
3. **界面拆分**：节点+环境 / 专家校验 两个标注界面，tab 互跳
4. **分类规则化**：标题关键词规则（剥代理机构名前缀防"工程管理/咨询"公司名污染），
   误判案例（囚车→货物、数字资源服务→服务）已修，每行展示命中关键词可溯源
5. **专家标注交互**：点选 verdict 绿色高亮 ≠ 提交；← 上一条 / 暂存(draft 列) / 提交 / 下一条 →
6. **项目总览入口化**：类别卡 → 项目 → 三类标注任务，实时待审数
7. **rubric 条目标注**：解析产物同款标注（保留/改写-文本写回/删除）

## 7. 当前成果

| 资产 | 数量 | 说明 |
|---|---|---|
| 招标项目 | 48 | ccgp 真实抓取，工程11/服务16/货物21（规则分类） |
| 标书 case | 5 | 4 个项目 + 1 个变体，全部 injected 状态 |
| 人工校验项 | 59 | N1-N4 节点 + 专家四区，内容取自真实招标文件要素 |
| 环境文件 | 34 | 含 what_is_it、证据/噪声/扫描件标记 |
| rubric | 5（44 条目） | 1 模板 + 4 项目专用，条目按 qual/comp/rej/score 分层 |
| 注入 GT | 4 | 财报年度/贴线报价/检测报告过期等，needs_review 状态 |

平台功能：项目总览入口、双标注界面、规则标注+编辑、PDF 阅读器（按页反馈）、
GT/裁决/附件校准队列，全部判定可撤销、可暂存。

## 8. 已知边界与下一步

- rubric 条目目前是手工种子，下一步用 OpenBidKit 18 解析项 schema 从真实 DOCX 自动抽取，
  其三轮废标检查 prompt 定为第一个评测基线
- 扫描件构造：参数化做旧（DPI/倾斜/阴影/压缩痕迹）vs 真实打印扫描，待定；
  平台已留"扫描件不真实"反馈通道收集专家判断
- agent 取证评测的工具面（shell vs 检索 API）、dynamic_events（澄清函发布）进二期
