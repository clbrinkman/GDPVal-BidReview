# 标书审查 Benchmark · 数据治理平台 — 项目讨论纪要 v1

> 日期：2026-09-01
> 状态：选题已收敛（做审查类），pipeline 框架已定，待验证最高风险环节

## 1. 目标

对标 GDPval 的思路，做一个**标书审查**领域的数据集 + 数据治理平台：
以尽可能少的人工，跑通 **选题 → 构造 → 质检** 的 human-in-the-loop 闭环。

## 2. 从 GDPval 借鉴什么

| GDPval 的做法 | 我们的取舍 |
|---|---|
| 任务是真实职业产出，评"交付物能不能用" | 审查报告（符合性审查表/废标项清单）就是真实交付物，成立 |
| 专家盲评 + pairwise 对比为主 | 只用于**校准自动指标**，不做主评测——我们有 ground truth，不用盲评兜底 |
| 任务分布对齐真实职业/行业分布 | 选题按行业（工程/货物/服务）、地区、金额分层抽样 |
| 没有标准答案，只能人评 | 我们走偏差注入，ground truth 免费获得 → 更像传统 benchmark，是升级不是缺陷 |

审查型任务合规性：GDPval 职业列表里本身就有 Compliance Officers / Auditors / Editors / Lawyers 这类"审别人产出"的职业，交付 findings 报告是合法形态。

**原则：别把"像 GDPval"当目标，把"评得准、人力省"当目标。**

## 3. 数据形态（已收敛）

- **做审查，不做写标书**。审查有确定性答案，可自动验证；写作只能靠专家盲评，人力成本高一个量级。
- **做资格性/符合性审查（逐条 yes/no），避开主观评分**（技术方案优劣打分裁量在专家，第一期不碰）。

## 4. 数据源（全部公开可得）

1. **招标文件**：政府采购网、各地公共资源交易中心。按 87 号令，评标办法必须写进招标文件并公开 → **每份标书自带当次 rubric**（资格性审查表、符合性审查表、评分标准表）。
2. **废标/中标公告**：写明"某投标人因 XX 原因未通过符合性审查"——真实审查员的产出，用于校准偏差注入 taxonomy，也可反向出题。
3. **财政部投诉处理决定/指导性案例**：边界 case 的官方裁量口径，适合做难题集。

## 5. Pipeline 设计

### 5.1 选题（自动化为主）
- 爬公开招标文件，按行业/地区/金额分层抽样，对齐真实审查员工作量分布。
- 人工只做一件事：确认标种"真实审查员确实会接到"，砍掉怪胎标。

### 5.2 构造（合成 + 程序化注入）
- 投标文件走合成：LLM 生成"完全合规"基线响应 → **程序化注入偏差**（证书过期、缺签章、星号条款不响应、业绩不满足、报价超上限等）。
- 注入是确定性的 → ground truth 免费。
- 偏差 taxonomy（20~30 类常见废标原因）人工打磨一次，从真实废标公告统计而来。

### 5.3 质检（自动指标 + 人工裁决）
- 主指标 pointwise：每条注入偏差是否被检出（检出率）、是否误报（误报率），全自动。
- GDPval 式 pairwise 盲评抽 5~10%，校准"自动指标 vs 人工判断"的相关性：
  - 相关性高 → 人工只做持续抽检；
  - 相关性掉 → 触发一轮人工复评。
- **人工收敛到两处：taxonomy 一次性打磨 + 分歧裁决。日常人力趋近于零。**

## 6. 数据治理平台功能模块（草案）

1. **采集层**：招标文件 / 废标公告爬取，PDF 解析。
2. **Rubric 结构化器**：从"评标办法"章节抽取结构化审查标准（LLM 任务 + 人工抽检兜底）。⚠️ 格式各地各行业差异极大，无统一 schema——**这是全链路不确定性最高的一环，也是最优先要验证的**。它本身也是个有用的中间件产物。
3. **构造引擎**：合规基线生成 + 偏差注入器（taxonomy 驱动，注入记录即 ground truth）。
4. **评测/质检台**：自动指标（检出率/误报率）、pairwise 盲评采样界面、分歧裁决工作台。
5. **数据资产层**：case 库、rubric 库、偏差 taxonomy 库、版本管理（含评测时的 case 组成控制——评测结论可比性的前提）。

## 7. 数据治理设计（2026-09-01 追加）

### 7.1 统一编号（层级式，号段即语义）
```
TDR-{来源}-{年份}-{序号}      招标文件
RBC-{tender_id}-v{n}          rubric（挂在 tender 下，表达从属）
CASE-{tender_id}-{variant}    测试用例
INJ-{case_id}-{err_code}      注入记录（= ground truth）
RUN-{date}-{model}-{caseset}  评测批次
```

### 7.2 标书分类（多维受控词表，锚定官方目录）
- **品目（主维）**：直接用财政部《政府采购品目分类目录》三级体系，不自造
- **采购方式**：公开招标/邀请招标/竞谈/磋商/询价（法定枚举）
- **标的类型**：工程/货物/服务
- 地区、金额区间为辅助切片维
- 分类走 LLM + 置信度路由，低置信进抽检队列；落库存 `classified_by` + 置信度

### 7.3 错误类型 taxonomy（层级编码，只增不改，旧 code 永不回收）
```
ERR-QUA  资格类       资质过期/等级不够、业绩不满足、财务不达标、信用记录、社保/纳税、联合体不合规
ERR-FMT  形式类       缺签章、密封不合规、份数不对、逾期递交、投标有效期不足、保证金问题
ERR-SUB  实质性响应类  星号(★)条款不响应、负偏离未声明、工期/质保不满足、付款方式不接受、强制认证缺失
ERR-PRC  报价类       超限价、报价缺项、大小写不一致、低于成本价(边界)
ERR-DOC  完整性类     文件缺失、授权委托不合规、证书造假(难题)
```
界外（做不了）：串标围标鉴定（MAC 地址一致、报价规律差异）——属评标委员会调查权，文本审查审不出来。

错误第二维：**按构造方式分**，直接决定人力等级
| 构造方式 | 典型错误 |
|---|---|
| P 程序化注入（GT 100% 确定） | 删签章、改有效期、超限价、大小写 |
| G 生成式注入（LLM 改写，偏差成立性需验证） | 参数负偏离、业绩缩水、工期不足 |
| R 真实案例改编（废标公告/财政部案例，裁量性强） | 低于成本价、"重大偏差"边界、证书真伪 |

### 7.4 异构 rubric 下的评分原则
- **永远不跨 rubric 比总分**；最小评分单位是 rubric 条目（item 级 yes/no）
- 聚合只做归一化比率（检出率/误报率），按错误类别×品目×标型切片，macro 平均防止条目多的标型主导
- 跨标可比性靠模板层的公共检查点（见 7.5）

### 7.5 Rubric 两层模型（关键设计）
```
rubric = template_base（官方示范文本层：九部委标准施工招标文件/财政部范本/各省示范文本，跨标复用）
       + delta（该标特有：星号条款、特定资质、项目特定要求）
```
收益：
1. 抽取先模板匹配，命中只抽 delta → 抽取成本骤降
2. 模板层检查点跨标统一，构成 benchmark"公共骨架"，模型表现严格可比；delta 层切片报
3. `rubric_template` 表 + 覆盖率本身是治理指标

### 7.6 HITL 人力分级与错误×项目矩阵（2026-09-01 追加）

**人力四级**：L0 零人工（P类）｜L1 抽检5~10%（P类需上下文联动自然的）｜L2 生成+人审（G类，审"偏差真成立吗/会误读吗"）｜L3 人工构造（R类，人写GT+理由，LLM只扩写）

| 错误类 | 工程 | 货物 | 服务 |
|---|---|---|---|
| FMT 形式 | L0~L1 | L0~L1 | L0~L1 |
| PRC 报价 | L0 | L0 | L1 |
| QUA 资格 | L2 | L1 | L1 |
| SUB 实质响应 | L2 | **L1~L2（结构化最高，最先做）** | **L3（最贵，第一期少做/只做真实案例）** |
| DOC 完整性 | L1 | L1 | L1 |

规律：格子结构化程度越高人力越低。**第一个端到端打通的格子选 货物×SUB（参数应答表）**。

**动态治理（人力预算优化器）**：
- 格子多轮 run 自动指标与抽检人判一致性 >95% → 降级撤人
- 裁决分歧率高或裁决员判"GT 本身有问题" → 升级加人 + 注入函数/prompt 进返修队列
- 裁决记录按 err_code × 项目类 聚合成分歧热力图 = 平台治理看板，人力分配由数据驱动

### 7.7 原始数据可抓性结论
- ✅ 可抓：招标文件（ccgp.gov.cn / 各省交易中心 / 中国招标投标公共服务平台）、中标公告、**废标/否决公告（taxonomy 语料金矿）**、财政部投诉处理公告
- ❌ 抓不到：投标文件本身（商业秘密）→ 必须合成，注入法成立的根本原因
- 风险：老标附件是扫描件（需 OCR，选题优先近2~3年电子化高的省份）；部分站登录/反爬；废标原因写法各地粗细不一，按"原因字段信息密度"过滤


## 8. 参考项目调研（2026-09-01）

### 8.1 zenml-io/awesome-open-data-annotation
纯工具清单无方法论。可借两点：Snorkel（标注函数编程式打标 = 我们"程序化注入=免费GT"的理论先例，可写进背景）；Label Studio 的"预标注+人工修正"交互模式，供裁决台 UI 参考。不引入其本体——人工触点太小。

### 8.2 FB208/OpenBidKit_Yibiao（本地镜像 C:\tmp\_ref_OpenBidKit，AGPL-3.0 ⚠️代码不可直抄）
AI 写标书桌面工具（Electron+SQLite+OpenXML(.NET)+MinerU 解析，架构与我们"本地优先"同构）。四层反推价值：

**① 18 解析项 = 生产验证过的招标文件解析 schema**（`client/src/features/technical-plan/services/bidAnalysisWorkflow.ts`）：
项目概述/技术评分要求/项目信息/甲方信息/交货和服务要求/采购清单/响应文件要求/代理机构信息/投标关键节点/投标保证金/**资格性审查**/**符合性检查**/开标要求/评标要求/商务评分要求/**无效标与废标项**/合同授予与签订/合同解除和终止。
加粗三项 = 我们的 rubric 核心。**rubric 结构化器 v1 schema 直接以此为底，再用真实招标文件验证裁剪。**

**② 废标检查三轮 LLM pipeline**（`client/src/shared/prompts/rejectionPrompts.ts`）：分析(圈定可判范围) → 检查(逐项出风险) → 定稿(去重出JSON)。
关键工程规则：排除电子文本不可判事项（签字/盖章/密封/现场）；"材料缺失"判定采纳结构性线索原则（目录/章节标题/材料清单/页码存在即不得判缺失）；每条风险必须附原文证据；invalidBid / rejectionItem 两型区分。
用途：(a) 原样跑作 benchmark 第一个被测 baseline；(b) 反哺注入器设计——ERR-DOC 缺失必须注入成"连目录线索都没有"，否则强模型正确拒判、GT 反而错。

**③ 生成链路 = 现成基线生成器**：解析招标→LLM写标→一致性检查→查重→导出 Word，质控件齐全。成本实证：11万字标书≈1元 token 费 → 基线批量生成成本可忽略。

**④ 警告：AGPL-3.0**，prompt 思路/schema 可参考，代码不能抄进我们平台。

## 9. 待办 / 风险

- [ ] **最先验证：rubric 抽取原型**——以 OpenBidKit 18 解析项为 v1 schema，不确定性已大幅下降
- [ ] 跑 OpenBidKit 废标检查 prompt 作第一个被测 baseline
- [ ] rubric 模板库冷启动：收集官方示范文本（九部委/财政部/重点省份）
- [ ] 偏差 taxonomy 初稿：从 N 份真实废标公告统计
- [ ] 真实投标文件获取渠道（哪怕几十份，用于校准合成数据分布，决定外部效度）
- [ ] 确认合成投标文件与真实文件的分布差（决定 benchmark 可信度）

## 10. 爬虫实测结论（2026-09-01，scraper/ccgp.py）

- **源站**：ccgp 搜索接口（search.ccgp.gov.cn）有 IP 频控（"访问过于频繁"拦截页），**弃用**；改用静态频道列表页（/cggg/dfgg/gkzb/index.htm 等，每页20条，index_{n}.htm 翻页），无拦截。
- **编码坑**：ccgp 页面实为 UTF-8，但 ASCII 结构用 GBK 解也不报错 → 静默乱码。必须 utf-8 优先、gb18030 兜底；Windows 控制台需 stdout.reconfigure(utf-8)。
- **附件命中率**：ccgp 地方公开招标约 1/3 详情页带附件；附件常为 zip 打包，内同时含 PDF+DOCX 双格式招标文件——**DOCX 直接可做 rubric 抽取输入，比 PDF 解析友好**。
- **产物**：data/raw/{TDR-CCGP-YYYY-NNNNN}/（detail.txt + 附件），元数据入 data/platform.db（tender/attachment 表），URL 唯一约束幂等重跑。
- 频道已备：地方公开 df-gkzb / 中央公开 zy-gkzb / 地方磋商 df-jzxcs。
- 下一步注意：附件不全是招标文件（也有委托协议、承诺函模板），抽取管线需按内容过滤（是否含"评标办法/资格性审查"章节）。



## 11. 平台 v0 落地（2026-09-01）

**GT 校验专门设计（三原则）**
1. GT 记录自描述：injection 行存全 before/after/locator/expected_finding，校验人只看一个 diff 就能判，无需理解整个 case
2. 校验状态机按 L 级路由：`auto`(L0 不进队列) → `needs_review`(L1 抽10%/L2 全量) → `confirmed`/`rejected`；L3 校验人≠撰写人；rejected 带原因进返修
3. 校验界面最小上下文（要求原文+注入diff+期望结论）+ 三键操作 + 耗时记录，目标每条 <30s

**已建组件**
- `platform/db.py` schema v1：rubric / rubric_item / bid_case / injection / run / finding / err_taxonomy（code 只增不改，active 停用不删）；attachment 加 is_tender_doc
- `platform/app.py` FastAPI 工作台（:8321）：看板 + 三队列——附件筛选（y/n/s 键盘操作，附 detail.txt 摘录+文件预览）、GT 校验（before/after diff 视图，自动计时）、裁决台（模型finding vs GT 双栏，model_right/gt_right/both）
- 端到端已验证：附件标记流转、GT 种子数据 → 校验 → confirmed + 耗时落库
- 裁决台输出即"分歧热力图"数据源（finding.adj_result 按 err_code×项目类聚合）

**下一步**：rubric 抽取器（18项schema，输入=已标记 is_tender_doc=1 的附件 DOCX）→ 产出进 rubric 表 → 人工抽检队列（第四个队列，复用 GT 校验界面模式）

## 12. 平台方向修正：后期人工 prototype（2026-09-01）

用户纠偏：平台主体不是采集期队列，是**后期人工三件事**——①审核生成文件 ②规则修改 ③质检。分类/附件识别全自动+抽检，不占人工（队列只是校准器：人标一小批→测规则准确率→达标撤人）。

**生成审核按关键节点组织**（点节点跳转校验，任一驳回即回炉，全过才进题库）：
- N1 输入解析：rubric 抽取对不对（抽取结果 vs 原文定位）
- N2 基线合规：生成标书是否真合规——关键值逐项核（报价≤预算/工期/有效期/资格材料）
- N3 偏差注入：= GT 校验队列（复用）
- N4 成稿质量：章节齐/无占位符残留/全文一致

已落地：`/review`（case 列表+节点状态灯）→ `/review/{case}`（节点卡片）→ `/review/{case}/{node}`（校验项+artifact 证据+通过/驳回带原因）；N3 页跳转 GT 队列。演示数据基于真实抓取的 TDR-CCGP-2026-00002（企业出海综合服务，预算535万/3年）。nav 重构：生成审核/规则编辑/GT质检/裁决为主，项目总览/附件筛选/主审配置降为次级。

## 13. 专家校验标注界面（2026-09-01）

AI 生成标书的审核分三层：**程序全自动**（数值比较/存在性/一致性/格式）→ **机器初查+抽检**（证书编号格式/日期逻辑/标准号检索）→ **必须专家**（四处）。基线审核严格度要高于一般标书——必须保证"零非注入偏差"，否则模型检出 AI 自带偏差会被错算误报，指标就脏了。

**四处专家校验区**（/expert，入口按项目类别 工程/货物/服务 分区，各带专属 verdict 枚举）：
1. `law` 法规引用真实性：存在且现行/已废止/编造/场景不适用（AI 幻觉重灾区）
2. `scheme` 方案答非所问/矛盾：无矛盾/应答表与方案矛盾/答非所问/漏项
3. `plaus` 虚构事实合理带：合理/偏假可接受/一眼假回炉（决定外部效度）
4. `inj` 注入偏差领域可信度：自然/可疑可判/不像真实偏差

数据落 review_check(node='EXP', expert_area, result=verdict, note)。演示数据覆盖 服务(出海服务)+货物(灭菌器/物证实验室) 三个 case。

## 14. 环境文件与取证评测形态（2026-09-01）

评测形态升级：从"喂全文"到**把 agent 扔进噪声环境自行取证审查**。env_file 表 = case 的环境资产：role（tender/bid/cert/credit/clarify/noise_old旧版/noise_misc无关）+ is_noise + **has_evidence（含注入偏差证据的文件——日后测 agent 取证召回率的 GT）**。

环境文件在标注界面（/expert/{case}）展示但**不做主审内容**，只留反馈入口：缺失/内容错误/噪声不合理/版本不对/其他 + 备注，逐条覆盖式 upsert（沿用 agi 错例库语义）。反馈占位的 reviewer='human'，不占主审流程。

## 15. 参考 LH pipeline + PDF 阅读器（2026-09-01）

参考 `M:\下载\lh_10tasks_refined_00-09_20260807\...\05_restaurant_launch`（human_schema.json + human_view.html + _人类反馈评注.md）。

**schema 抄四样**：①key_information 的 source/golden_path（GT 带取证路径）→ 我们 has_evidence 升级为 证据文件→页码/章节→定位；②env.files[].what_is_it → env_file 已加 what_is_it 列；③反馈归类 taxonomy（E1载体不全/E2核心值冲突/I5指令模糊/I6提示泄露/C3计数口径）→ 我们反馈枚举照此细化；④dynamic_events 触发式动态事件 → 二期做"澄清函发布/截止前更正"动态难题。

**UI 抄 human_view**：/case/{case_id} 统一标注视图——左 sticky sidebar 目录树（GEN生成节点/EXP专家校验/ENV环境文件 三组 details 折叠 + pill 待办计数 + 锚点跳转 :target 绿色高亮），右卡片流，**每卡片内嵌反馈控件**（human_view 是只读的，我们的差异点）。真实招标文件 PDF 已接线（00002 zip 解出 419KB）。

**PDF 阅读器** /viewer/{env_file_id}：原生浏览器渲染（扫描件图片型PDF兼容）+ 右侧反馈面板（类型+页码+备注）+ 同case文件切换。env_file 加 fb_page / is_scan 列，反馈类型加"扫描件不真实"。

**扫描件构造待讨论**：参数化做旧（DPI/倾斜角/阴影/压缩伪影/噪声）vs 真打印扫描；构造参数当注入维度，专家反馈"像不像"驱动调参——这本身是个小 HITL 循环。
