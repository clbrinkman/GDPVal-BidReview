# agent 交互记录

标书审查 Benchmark + 数据治理平台与 agent 协作的完整记录（2026-08 至 2026-09）。
分两部分：A. 数据构造→质检全流程的设计与实现状态；B. 交互过程中的关键决策点。

---

## A. 数据构造 → 质检 全流程

平台覆盖的 pipeline：**选题 → 构造 → 质检**，人工以"后期人工"身份只出现在质检侧。

### A1. 选题（已实现）

- 爬虫 `scraper/ccgp.py` 抓中国政府采购网公开招标频道：频道列表页 → 详情页 → 附件下载，
  产物 `data/raw/{tdr_id}/`（detail.txt + 附件），元数据入 `tender`/`attachment` 表
- 规模：48 个项目、26 个附件
- 项目分类（工程/货物/服务）走**关键词规则**，不设人工：只取标题、剥掉「XX招标代理公司关于」前缀
  防公司名污染（"工程管理有限公司""项目咨询"），命中关键词在 UI 可溯源
  - 修过的误判：囚车更新→货物（车辆）、数字资源更新→服务、科研设备→货物（穿透咨询前缀）
- 踩坑记录：搜索接口频控弃用改静态频道页；UTF-8 页面按 GBK 解码不报错导致静默乱码入库
  （改 utf-8 优先 + gb18030 兜底并清库重抓）；Windows 控制台打印私有区字符崩溃

### A2. 构造（部分实现，种子演示阶段）

设计：`真实招标文件 → rubric 解析 → 合规基线标书 → 注入偏差 → 自带 GT`

- **rubric 两层**：template（通用审查项）+ delta（项目专用，从需求文档解析）。
  现状：5 个 rubric、44 条目为手工种子（取自真实招标要素：承诺函、一年一签、财报6个月内、
  中小企业声明函、限价同额、20日历天等）；**自动抽取器未实现**（规划用 OpenBidKit 18 解析项 schema）
- **基线标书**：未实现（规划：按 rubric 逐条生成合规响应）
- **注入**：设计了三种构造方式 P 程序化 / G 生成式 / R 真实案例，错误法典五类
  QUA 资质 / FMT 格式 / SUB 实质响应 / PRC 报价 / DOC 文档；现状 4 条手工注入种子
  （财报年度不符、贴线报价、检测报告过期、业绩数量不足）
- **环境文件**（agent 取证评测的噪声环境）：34 个文件种子，角色 tender/bid/cert/credit/clarify/
  noise_old/noise_misc，含 what_is_it 说明、证据/噪声/扫描件标记；**扫描件做旧构造未实现**

### A3. 质检（已实现，平台主体）

人工质检分四层，全部支持**暂存（draft）/提交/撤销回退**：

1. **生成节点审核 N1-N4**（/case/{id}）：N1 输入解析 → N2 基线合规 → N3 偏差注入 → N4 成稿质量。
   每个校验项一张卡：证据原文 + 通过/驳回 + 驳回原因。任一节点驳回即回炉，全通过进题库。
   现状 59 条校验项（内容取自真实招标文件）
2. **N3 = GT 校验**（/queue/gt）：injection 的 gt_status 状态机
   auto → needs_review → confirmed/rejected，计时收集
3. **专家四区**（/case/{id}/expert）：机器替代不了的判断——
   law 法规引用真实性（存在且现行/已废止/编造/场景不适用）、
   scheme 方案矛盾（无矛盾/应答表与方案矛盾/答非所问/漏项）、
   plaus 虚构合理带（合理/偏假可接受/一眼假回炉）、
   inj 注入可信度（自然/可疑但可判/不像真实偏差）。
   交互：点选 verdict 绿色高亮 ≠ 提交；← 上一条 / 暂存 / 提交 / 下一条 →
4. **规则条目标注**（/rubrics/{id}/annotate）：解析产物的人工校验——保留/改写（文本直接写回
   rubric_item）/删除，同款暂存/提交/上一条下一条交互
5. **环境文件反馈**（非主审）：反馈法典 缺失/内容错误/噪声不合理/版本不对/扫描件不真实/其他，
   PDF 阅读器原生渲染（兼容扫描件）+ #page=N 页码级反馈，可清除

另有裁决队列（/queue/adjudication）：模型 finding 与 GT 冲突时人工裁 model_right/gt_right/both/neither。

### A4. 评测（未实现）

run/finding 表已建：RUN-{date}-{model}-{caseset} 批次、hit/miss/false_positive 自动对 GT。
指标设计：逐条检出率/误报率，按错误类型 × 项目类别宏平均切片，只在同版本 case_set 上比。

---

## B. 交互过程关键决策点

1. **立项**：GDPval（职业交付物+盲评对拍）平移讨论 → 做审查类不做生成类；
   真实标书不公开 → 注入式合成，GT 免费（自描述：位置/改前/改后/期望结论），
   人工不"找答案"只"验答案"，目标单条 <30 秒
2. **治理先行**：编号六前缀（TDR/RBC/CASE/INJ/ERR/RUN）、错误法典、逐条评分不跨 rubric 合总分、
   HITL 矩阵（错误类型 × 项目类别 × P/G/R × L0-L3）
3. **平台定位纠偏**（用户"你做错了"）：第一版做成采集人工队列 → 纠正为后期人工 prototype，
   只有审核生成/规则编辑/质检三件事；分类与识别走规则（标定小样本→测规则准确率→撤人）
4. **参考 LH pipeline**：human_schema 的 key_information.source/golden_path（证据路径）、
   env.files.what_is_it、反馈法典；human_view 视觉词汇先整套复刻（sidebar 目录树/锚点绿条/
   stage-card/琥珀备注卡/三色 item 条）后按"越简单越好"削掉技术 ID 装饰
5. **迭代**：撤销回退 → 编号入 README → 节点/专家界面拆分 → 分类规则化 →
   专家选中/暂存/提交交互 → 项目总览入口化 → rubric 条目标注 → 打包上传 GitHub

## 当前成果速览

48 tender ｜ 5 case ｜ 59 review_check ｜ 34 env_file ｜ 5 rubric（44 条目）｜ 4 injection
仓库：https://github.com/clbrinkman/GDPVal-BidReview
